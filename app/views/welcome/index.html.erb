<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../assets/stylesheets/foundation-icons/foundation-icons.css" />
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

<body>
  <%= render 'nav/index' %>
  <%= render 'layouts/flash' %>

  <div class="homescreen">
    <img src="/assets/background.png" alt= "dna" />

    <div class="small-2 large-3 card" id="login-box">
      <%if current_user.nil? -%>
      <div class="labels">
        <%= form_for(:user, :url => session_path(:user)) do |f| %>
        <%= f.label :login %>
        <%= f.text_field :login %></br>

        <%= f.label :password %>
        <%= f.password_field :password %></br>

        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %></br>

        <%= f.submit 'Sign In' %></br>

        <%= link_to "Forgot your password?", new_password_path(:user) %></br>

        <%= link_to "Sign Up", new_user_registration_path %>
        <% end %>
      </div>
      <%end %>
    </div>

  </div>

</body>
