<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../assets/stylesheets/foundation-icons/foundation-icons.css" />
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render 'layouts/navbar' %>
    <%= render 'layouts/flash' %>
    <div class="row">
      <div class="container">
        <div class="column">
          <div class="small-6 medium-9 large-12 columns">
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
  </body>
</html>
